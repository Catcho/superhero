
extends ./layout.jade
block content

  .container
    .starter-template
      h1= title
      h2= message
      p.lead
        | Now you can start your own project with
        a(target='_blank', href='http://getbootstrap.com/') Bootstrap 3.3.6
        | . This plugin is a fork from
        a(href='https://github.com/le717/brackets-html-skeleton#readme') HTML Skeleton
        | .
      .row(data-ng-controller='priceController')
        .col-xs-3
          input.form-control(data-ng-model='yourPrice')
          hr
          p A te árad: {{calcOwnPrice(yourPrice) | currencyFilter}}
          p Más ára: {{calcOtherPrice(yourPrice) | currencyFilter}}
          p Teljes ár: {{yourPrice | currencyFilter}}
      .row(data-ng-controller='userController')
        .col-xs-3
          p {{yourPrice}}

      ul
        each user in users
          li #{user.id}: #{user.name} - #{user.email}

